// Aqui deberia ir una funcion que analice el path 

// reading file
fs.readFile('/etc/passwd', (err, data) => {
    if (err) throw err;
    console.log(data);
  });
 fs.readdir(path[options], callback);
 path.resolve([...paths])
 
const route = process.argv;

const convertPath = (route) => {
    console.log(route)
}